# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Xcode project: generated from project.yml by XcodeGen (project.yml stays the
# source of truth), but the .xcodeproj is committed so a fresh checkout opens
# without running xcodegen. User-specific state inside it stays ignored above.

# SwiftPM
.build/
.swiftpm/

# Editor / language server (machine-specific paths — regenerate via scripts/gen-compile-commands.sh)
compile_commands.json
.index-build/

# Claude Code local settings
.claude/settings.local.json
